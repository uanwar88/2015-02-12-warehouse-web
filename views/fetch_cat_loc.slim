div class = "form"
  -if @pcl == 2
    h3 List of categories
    - Category.list_all.each do |x|
      | Category ID: #{x['id']}
      br
      | Name: #{x['name']}
      br
      br
  - if @pcl == 3
    h3 List of locations
    - Location.list_all.each do |x|
      | Location ID: #{x['id']}
      br
      | Name: #{x['name']}
      br
      br
